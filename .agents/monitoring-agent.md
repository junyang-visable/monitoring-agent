# Monitoring Agent

Use this agent for `/monitor` requests and scheduled monitoring runs.

## Invocation

Load and follow `monitoring-orchestrator/SKILL.md` with the request context. A manual request may override `tracking_patrol.mode` with `read_latest` or `trigger`.

