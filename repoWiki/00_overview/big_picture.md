# Big Picture

- System → configuration-driven, multi-project monitoring agent.
- Core flow → config → parallel signal calls → normalized results → reports and redacted evidence.
- Constraints → real external calls; no fabricated data; independent per-signal failure handling.
