# Development Guide

## Steps

1. Configure external service credentials as environment variables.
2. Load a project configuration.
3. Run the monitoring entry point.
4. Inspect report and evidence artifacts.

## Commands

- install → not defined.
- server → not defined.
- test → not defined.

## Environment Variables

- names → to be defined by adapters; values must never be committed.

